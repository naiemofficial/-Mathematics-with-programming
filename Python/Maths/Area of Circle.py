import math
radius = float(input("Enter the radius of circle: "))
area = 3.1416 * radius * radius  # or, area = math.pi * radius * radius (must use 'import math')
print("The area of circle is =", round(area, 2))